apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: alphintra-ingress-gateway
  namespace: istio-system
spec:
  servers:
    - hosts:
        - "staging.alphintra.dev"
        - "staging.alphintra.cloud"
    - hosts:
        - "staging.alphintra.dev"
        - "staging.alphintra.cloud"
      tls:
        credentialName: staging-alphintra-gateway-tls
