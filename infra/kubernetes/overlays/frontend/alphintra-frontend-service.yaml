apiVersion: v1
kind: Service
metadata:
  name: alphintra-frontend-service
  namespace: default
  labels:
    app: alphintra-frontend
    environment: dev
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: alphintra-frontend