# Test Environment Configuration

# Application Settings
ENVIRONMENT=test
PORT=8002
DEBUG=true
LOG_LEVEL=DEBUG

# Security Settings
SECRET_KEY=test-secret-key
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database Configuration - Using SQLite for tests
DATABASE_URL=sqlite+aiosqlite:///./test.db
TIMESCALE_DATABASE_URL=sqlite+aiosqlite:///./test_timeseries.db
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# Redis Configuration - Using in-memory for tests
REDIS_URL=redis://localhost:6379/15
REDIS_POOL_SIZE=5

# Google Cloud Platform - Mock values for tests
GCP_PROJECT_ID=test-project
GCP_REGION=us-central1
GCS_BUCKET_NAME=test-bucket
VERTEX_AI_LOCATION=us-central1

# MLflow Configuration - Using local file storage
MLFLOW_TRACKING_URI=file:./mlruns
MLFLOW_EXPERIMENT_NAME=test-experiment

# Kafka Configuration - Using mock in tests
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_CONSUMER_GROUP=test-ai-ml-strategy-service

# External Service URLs - Using mock servers for tests
AUTH_SERVICE_URL=http://test-auth-service
TRADING_ENGINE_URL=http://test-trading-engine
MARKET_DATA_SERVICE_URL=http://test-market-data

# Test Settings
TESTING=true
CODE_EXECUTION_TIMEOUT=60
MAX_DATASET_SIZE_MB=10
MAX_TRAINING_TIME_HOURS=1
MAX_BACKTEST_YEARS=1
DEFAULT_INITIAL_CAPITAL=10000.0
