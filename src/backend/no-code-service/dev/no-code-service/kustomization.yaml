apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - pdb.yaml
  - hpa.yaml

labels:
  - pairs:
      app: no-code-service
      component: backend
      tier: service

namespace: alphintra