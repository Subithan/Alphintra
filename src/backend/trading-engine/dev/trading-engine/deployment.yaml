apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading-engine
  namespace: alphintra
  labels:
    app: trading-engine
    component: backend
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trading-engine
  template:
    metadata:
      labels:
        app: trading-engine
        version: v1
        component: backend
        environment: dev
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8008"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: trading-engine
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        # Trading Engine Application Container
        - name: trading-engine
          image: gcr.io/alphintra-472817/trading-engine:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8008
              protocol: TCP
          envFrom:
            - configMapRef:
                name: trading-engine-config
            - secretRef:
                name: trading-engine-secrets
          env:
            # Override database URL to use Cloud SQL proxy
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: trading-engine-secrets
                  key: DATABASE_URL
            # Database username from secret
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: trading-engine-secrets
                  key: SPRING_DATASOURCE_USERNAME
            # Database password from secret
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trading-engine-secrets
                  key: SPRING_DATASOURCE_PASSWORD
            # Redis password from secret
            - name: SPRING_DATA_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trading-engine-secrets
                  key: SPRING_DATA_REDIS_PASSWORD
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 2Gi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 120
            failureThreshold: 12
            periodSeconds: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: app-logs
              mountPath: /app/logs
            - name: tmp
              mountPath: /tmp

        # Cloud SQL Proxy Container
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.10.1
          args:
            - "--port=5432"
            - "--address=127.0.0.1"
            - "--private-ip"
            - "--credentials-file=/secrets/service-account.json"
            - "alphintra-472817:us-central1:alphintra-db-instance"
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 50m
              memory: 128Mi
          volumeMounts:
            - name: service-account-secret
              mountPath: /secrets/
              readOnly: true

      volumes:
        - name: service-account-secret
          secret:
            secretName: trading-engine-sa-key
        - name: app-logs
          emptyDir: {}
        - name: tmp
          emptyDir: {}
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
