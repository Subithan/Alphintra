spring.profiles.active=${SPRING_PROFILES_ACTIVE:standalone}
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://postgres:5432/alphintra}
spring.datasource.username=alphintra
spring.datasource.password=${DB_PASSWORD:alphintra123}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.redis.host=${REDIS_HOST:redis}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD:redis123}
spring.kafka.bootstrap-servers=kafka:9092
management.endpoints.web.exposure.include=health,info